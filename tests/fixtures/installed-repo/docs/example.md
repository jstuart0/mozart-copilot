# Example docs

A placeholder file so `tests/fixtures/installed-repo/docs/` is a real,
non-empty directory the install gate can assert still exists (and is
untouched) after `scripts/install-bundle.sh --target` runs.
